//! Groups reusable UI and debug overlay drawing.
//!
//! System: UI layer. This module owns overlay composition helpers; scene state
//! and gameplay rules stay in `scenes`, `game`, and `combat`.

pub mod binary_text;
pub mod combat_debug;
