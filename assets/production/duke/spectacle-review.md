# Duke — signature spectacle and combat reactions

System.out.println! performs a paper-roll reveal, printer construction and expansion, sustained receipt barrage, comic paper jam, collapse, and return to guard. Native rendering supplies the exact signature title. Actor durations are 130/150/153 ms startup, 167/183 ms active, then 250/267/367 ms recovery (1667 ms total). Actor scale is 0.70. FX are center-anchored with source scales 0.35 projectile and 0.34 impact. The projectile is roughly 110–125 px wide and the largest burst roughly 170 px wide.

The original transparent signature source is retained directly. Revised magenta-matte sources preserve white paper and steam that the rejected checkerboard extraction damaged. Heavy-hit v1 had a black background and was replaced. The last launched drawing was uniformly enlarged by 1.2 to match the other drawings' body scale; its crop, packing position and pivot are documented in [launched/normalization-spectacle.json](launched/normalization-spectacle.json). No anatomy or pose was synthesized during preparation. The 63 unaffected baseline frames are byte-preserved.

All new poses and effects were generated with the built-in imagegen tool from the established character reference. Prompt/source versions and SHA-256 hashes are recorded in [spectacle-generation.json](spectacle-generation.json). Earlier rejected renders remain available. Local matte removal, explicit crops and uniform scaling follow the existing user authorization in the production README.

The actor sequence contains eight signature drawings, six throw drawings, four thrown reactions, four launched reactions and three heavy-hit reactions. The independent FX atlas contains two looping projectile drawings and four once-only impact drawings. Runtime actor timings match the combat contract: throw 800 ms; captured victim 200 ms then 180/200/300 ms of flight; launched reaction 120/180/180/300 ms; heavy hit 90/140/170 ms. Hitstun/airborne state determines whether the full reaction plays.

The sheets were inspected after preparation and atlas export. Unequal imagegen layout boundaries required explicit recuts for aerial poses and FX; using an assumed equal grid had included adjacent fragments. The selected rectangles now follow the actual gaps. Both FX sheets were inspected at runtime size on dark and light backgrounds: paper, steam, bright C memory edges and internal shading remain visible, without checkerboard/magenta backgrounds or neighboring frame fragments. The atlas uses a fixed scale and explicit pivots; per-frame fitting is not used.

Baseline preservation was checked by comparing each unchanged frame's RGBA bytes, dimensions, duration, pivot, clip, trimmed bounds and combat metadata against the saved pre-change atlas. See [spectacle-preservation.json](spectacle-preservation.json). Atlas positions may change during packing. Only the old signature/throw clips were replaced; all other existing clips retain their pixels and metadata.

Actual isolated Raylib scenes have now been inspected in both directions. The accepted report, exact clip coverage, capture metadata and 12 retained PNGs are in [the extraordinary review](../../candidates/duke/review/extraordinary/report.json). Full raw captures remain outside Git under `/tmp/borrow-fighters-duke-c-spectacle-final`; the report records their paths and hashes. All 25 new actor drawings were exercised by real contact/reaction playback. Java paper emission follows the printer mouth; C rupture meets the book at the floor. Throws cross behind the attacker, use inverted/rolling victim poses and settle into ground recovery. The old static grip defect is excluded by the revised Duke lift-v3 capture evidence.

Runtime review found Duke’s third throw pose kept its hands near its waist while the victim rose. Built-in imagegen revision v3 now raises both arms to support the opponent overhead. Only that full source rectangle was replaced; the other five pose crops retain v2 bytes. See [mapping-spectacle-v3.json](throw/mapping-spectacle-v3.json) and the retained left/right supporting-lift PNGs. Alpha was inspected again on light/dark backgrounds.
